import { StyleSheet, Text, View } from "react-native";
import React from "react";

const CustomCarousel = () => {
  return (
    <View>
      <Text>CustomCarousel</Text>
    </View>
  );
};

export default CustomCarousel;

const styles = StyleSheet.create({});
